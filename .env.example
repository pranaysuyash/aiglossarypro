# =================================================================================
# AI/ML Glossary Pro - Environment Configuration
# =================================================================================
# Copy this file to .env and fill in your actual values
# DO NOT commit the .env file to version control

# =================================================================================
# REQUIRED ENVIRONMENT VARIABLES
# =================================================================================

# Database Configuration (Required)
# PostgreSQL connection string
DATABASE_URL=postgresql://username:password@hostname:5432/database_name

# Session Configuration (Required)
# Generate a secure session secret (use a random string generator)
SESSION_SECRET=your-super-secure-session-secret-key-at-least-32-characters

# Application Configuration (Required)
NODE_ENV=development
PORT=3000

# =================================================================================
# PRODUCTION MONITORING & ANALYTICS (Required for Production)
# =================================================================================

# PostHog Analytics (Required for user behavior tracking)
VITE_POSTHOG_KEY=your-posthog-project-api-key
POSTHOG_HOST=https://app.posthog.com

# Google Analytics 4 (Required for web analytics)
VITE_GA4_MEASUREMENT_ID=G-XXXXXXXXXX
VITE_GA4_API_SECRET=your-ga4-measurement-protocol-api-secret

# Sentry Error Tracking (Required for error monitoring)
SENTRY_DSN=your-sentry-dsn-url
SENTRY_DSN_DEV=your-sentry-dev-dsn-url-optional

# =================================================================================
# OPTIONAL ENVIRONMENT VARIABLES
# =================================================================================

# AWS S3 Configuration (Optional - for file storage and processing)
# If not provided, S3 functionality will be disabled
AWS_ACCESS_KEY_ID=your-aws-access-key-id
AWS_SECRET_ACCESS_KEY=your-aws-secret-access-key
AWS_REGION=us-east-1
S3_BUCKET_NAME=your-s3-bucket-name

# PostgreSQL Direct Configuration (Optional - alternative to DATABASE_URL)
# These can be used instead of or in addition to DATABASE_URL
PGDATABASE=database_name
PGHOST=hostname
PGPORT=5432
PGUSER=username
PGPASSWORD=password

# OpenAI API Configuration (Optional - for AI-powered features)
# If not provided, OpenAI functionality will be disabled
OPENAI_API_KEY=sk-your-openai-api-key

# Redis Configuration (Optional - for job queues and caching)
# If not provided, mock Redis client will be used in development
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=your-redis-password
REDIS_DB=0
REDIS_URL=redis://localhost:6379
REDIS_ENABLED=false

# Email Configuration (Required for Production)
# Required for sending job completion notifications and newsletters
EMAIL_ENABLED=true
EMAIL_SERVICE=smtp
EMAIL_FROM=noreply@aiglossarypro.com
EMAIL_FROM_NAME="AI Glossary Pro"

# SMTP Configuration (choose one)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password

# Alternative: Gmail App Password
# EMAIL_USER=your-email@gmail.com
# EMAIL_APP_PASSWORD=your-16-char-app-password

# Alternative: SendGrid (recommended for production)
# SMTP_HOST=smtp.sendgrid.net
# SMTP_PORT=587
# SMTP_USER=apikey
# SMTP_PASSWORD=your-sendgrid-api-key

# Google Drive API Configuration (Optional - for Google Drive integration)
# If not provided, Google Drive functionality will be disabled
GOOGLE_DRIVE_API_KEY=your-google-drive-api-key

# Replit Authentication Configuration (Optional - for Replit hosting)
# Required only when deploying to Replit or using Replit auth
REPLIT_CLIENT_ID=your-replit-client-id
REPLIT_CLIENT_SECRET=your-replit-client-secret
REPLIT_DOMAINS=your-replit-domain.replit.dev
REPL_ID=your-repl-id
ISSUER_URL=https://replit.com/oidc

# Gumroad Monetization Configuration (Required for Production)
# Required for processing payments and webhook verification
GUMROAD_WEBHOOK_SECRET=your-gumroad-webhook-secret-key
BASE_URL=https://your-domain.com

# Security Configuration (Recommended for Production)
# Rate limiting configuration
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100
MAX_FILE_SIZE=10485760

# CORS Configuration
CORS_ORIGIN=https://your-domain.com

# =================================================================================
# DEVELOPMENT vs PRODUCTION NOTES
# =================================================================================

# Development:
# - Use NODE_ENV=development
# - PORT can be any available port (3000, 5000, etc.)
# - Database can be local PostgreSQL or cloud-hosted
# - S3 and other services are optional for basic functionality

# Production:
# - Use NODE_ENV=production
# - PORT should be set by your hosting platform or use 5000 for Replit
# - All database credentials must be properly configured
# - Consider enabling S3 for file processing in production
# - Replit auth variables are required if deploying to Replit

# =================================================================================
# SECURITY NOTES
# =================================================================================

# 1. Never commit your .env file to version control
# 2. Use strong, unique secrets for SESSION_SECRET
# 3. Rotate API keys regularly
# 4. Use environment-specific databases (separate dev/prod)
# 5. Consider using AWS IAM roles instead of access keys in production
# 6. Enable SSL/TLS for database connections in production

# =================================================================================
# SETUP INSTRUCTIONS
# =================================================================================

# 1. Copy this file to .env in the same directory
# 2. Fill in at minimum the required variables (DATABASE_URL, SESSION_SECRET)
# 3. Configure optional services as needed for your use case
# 4. Run `npm run dev` to start the development server
# 5. Check the console output for configuration status

# Need help? Check the documentation or logs for configuration status messages.